import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'portfolio-2022',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
